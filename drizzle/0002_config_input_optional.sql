-- Конфигурация может начинаться с одной геометрии: пользователь рисует трассу
-- в конструкторе, а электрическую нагрузку привязывает позже.
-- Требовать input_json с первой версии значило бы заставлять выдумывать данные.
ALTER TABLE config_versions ALTER COLUMN input_json DROP NOT NULL;
