import "server-only";

/**
 * Точка входа в базу для кода приложения.
 *
 * server-only ломает сборку, если этот модуль попытаются импортировать из компонента
 * с "use client": строка подключения и схема не должны попадать в клиентский бандл.
 * Прикладной код ходит сюда не напрямую, а через слой доступа lib/dal — там проверка
 * сессии и обязательный фильтр по тенанту (ТЗ 12.3.3).
 */
export { db, sql, schema } from "./client";
